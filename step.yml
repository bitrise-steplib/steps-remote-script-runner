name: Download Script
description: |
  Bitrise step that downloads the content from the input url and runs it as a bash script.
  For example you can provide a Gist url.

inputs:
  - title: Bash Script URL which returns a *text* response (not an HTML one!)
    mapped_to: DOWNLOAD_SCRIPT_URL
